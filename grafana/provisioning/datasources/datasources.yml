apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: alkaline
      defaultBucket: experiments
      tlsSkipVerify: true
    secureJsonData:
      token: administrativetoken123
    editable: true
